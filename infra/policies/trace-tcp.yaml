apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: "trace-tcp-connect"
spec:
  kprobes:
  - call: "tcp_connect"
    syscall: false
    args:
    - index: 0
      type: "sock" # Contient IP source/dest et Ports
